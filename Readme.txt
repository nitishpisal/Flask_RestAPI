Simple RestApi using Python Flask

localhost:5000/uploader: Simple page for uploading files

localhost:5000/api/parse: uses the uploaded file, parse the text file and upload json file in
project folder. It calls api/sample service internally to get the response

localhost:5000/api/sample: is used to view the parsed json object from file (cms_sample.txt)